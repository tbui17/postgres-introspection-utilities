/** @type {import('prettier').Config} */
const config = {
	plugins: [],
	tabWidth: 4,
	useTabs: true,
	singleQuote: false,
	trailingComma: "es5",
	printWidth: 80,
	quoteProps: "as-needed",

	semi: false,
}

export default config
